version: '3.8'

services:
  db:
    image: postgres:14-alpine
    container_name: url-shortener-db
    environment:
      - POSTGRES_USER=myuser
      - POSTGRES_PASSWORD=mypassword
      - POSTGRES_DB=urlshortener
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myuser -d urlshortener"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Docker.yml # Make sure to point to your custom Dockerfile name!
    container_name: url-shortener-app
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - DB_HOST=db       # This matches the database service name below
      - DB_PORT=5432     # Inside the Docker network, use the internal port (5432)
      - DB_USERNAME=myuser
      - DB_PASSWORD=mypassword

volumes:
  postgres-data: